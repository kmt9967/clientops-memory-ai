# Hackathon requirements check

Checked August 15, 2026. The official Devpost rules hostname was indexed but intermittently failed DNS in Chrome. The authoritative indexed rules require a newly created agentic application, CockroachDB as meaningful persistent memory, at least one AWS service, and a public demonstration video under three minutes. The indexed page does **not** state that two CockroachDB tools or Managed MCP are mandatory.

Implementation mapping: persistent CockroachDB SQL + VECTOR memory; Amazon Bedrock reasoning and embeddings; new isolated repository; evidence-first agent workflow; synthetic demo; under-three-minute script. Final submission remains intentionally unsubmitted.

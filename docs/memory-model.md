# Memory model

Every memory has a type, title, content, actor, source, confidence, status, timestamps, and optional metadata. Decision and task detail lives in normalized tables. `memory_links` expresses reinforcement, contradiction, derivation, and superseding relationships. Historical records are preserved; inaccurate records are visible but excluded from answer retrieval.
